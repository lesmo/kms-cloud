@{
    LayoutValues layoutValues = ViewData["LayoutValues"] as LayoutValues;
    TipsValues tipsValues     = ViewData["TipsValues"] as TipsValues;
}

<div id="tipsGodines">
    <span class="tituloGrafica">
        <h2>
            <img src="~/Images/Gui/Gui.Day.jpg"> @LayoutStrings.TipOfTheDay.ToUpper()
        </h2>
        <hr />
    </span>
    <div class="tipGodin">
        <figure>
            <img src="@layoutValues.TipOfTheDay.IconUri.AbsoluteUri" alt="">
        </figure>
        <article>
            <h2>@layoutValues.TipOfTheDay.Category</h2>
            <p>@layoutValues.TipOfTheDay.Text</p>
            <ul class="redes">
                <li><a href="#" class="corona"></a></li>
                <li><a href="#" class="facebook"></a></li>
                <li><a href="#" class="twitter"></a></li>
            </ul>
        </article>
    </div>

    <span class="tituloGrafica">
        <h2>
            <img src="~/Images/Gui/Gui.Day.jpg">@TipsStrings.Categories.ToUpper()
        </h2>
        <hr>
    </span>
    <div id="Navegacion-Tips">
        <nav>
            <ul>
                @foreach ( TipCategoryModel category in tipsValues.Categories ) {
                    if ( category.CategoryId == tipsValues.CurrentCategory.CategoryId ) {
                        <li class="activo">
                            <img src="@category.IconUri.AbsoluteUri" alt="@category.Name" />
                        </li>
                    } else {
                        <li>
                            <a href="@Url.Action("Tips", "Index", new { cat = category.CategoryId })">
                                <img src="@category.IconUri.AbsoluteUri" alt="@category.Name" />
                            </a>
                        </li>
                    }
                }
            </ul>
        </nav>

        <div id="contenidosAjax">
            @foreach ( TipModel tip in tipsValues.CurrentCategoryTips ) {
                <article class="tips-items">
                    <div class="redesTips">
                        <ul class="redes">
                            <li><a href="#" class="corona"></a></li>
                            <li><a href="#" class="facebook"></a></li>
                            <li><a href="#" class="twitter"></a></li>
                        </ul>
                    </div>
                    <div class="contenido-tips">
                        <h3>@tip.Category</h3>
                        <p>@tip.Text</p>
                    </div>
                </article>
            }

            <div class="paginacion">
                <ul>
                    <li>
                        <a href="@Url.Action("Tips", "Index", new {
                            cat = tipsValues.CurrentCategory.CategoryId,
                            page = 1
                        })">«</a>
                    </li>
                    <li>
                        <a href="@Url.Action("Tips", "Index", new {
                            cat = tipsValues.CurrentCategory.CategoryId,
                            page = 1
                        })">1</a>
                    </li>
                    @for ( int i = 2; i <= tipsValues.CurrentCategoryTipsTotalPages; i++ ) {
                        <li>
                            <a href="@Url.Action("Tips", "Index", new {
                                cat = tipsValues.CurrentCategory.CategoryId,
                                page = i
                            })">@i</a>
                        </li>
                    }
                    <li>
                        <a href="@Url.Action("Tips", "Index", new {
                            cat = tipsValues.CurrentCategory.CategoryId,
                            page = tipsValues.CurrentCategoryTipsTotalPages
                        })">»</a>
                    </li>
                </ul>
            </div>
        </div>
    </div>
</div>
